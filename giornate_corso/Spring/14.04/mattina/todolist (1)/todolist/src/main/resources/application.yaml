spring:
  application:
    name: todolist

  datasource:
    url: jdbc:mysql://localhost:3306/todolist
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
