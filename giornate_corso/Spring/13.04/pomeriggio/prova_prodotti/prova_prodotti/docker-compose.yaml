version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: prova-prodotti:latest
    container_name: prova-prodotti-app
    ports:
      - "8080:8080"
    restart: unless-stopped
